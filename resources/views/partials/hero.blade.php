<section id="hero">
    <div class="hero-media">
        <img src="{{ asset('imgs/cherryFlowersDark.jpg') }}" alt="{{ __('site.hero.image_alt') }}">
    </div>

    <div class="hero-overlay">
        <div class="hero-shell">
            <div class="hero-content">
                <h1>{{ __('site.hero.title') }}</h1>
                <p class="hero-subtitle">{{ __('site.hero.subtitle') }}</p>
                <a href="#productos" class="hero-btn">{{ __('site.hero.cta') }}</a>
            </div>
        </div>
    </div>
</section>
